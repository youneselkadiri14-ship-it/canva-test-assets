export class FakeAutofillClient {
    async requestAutofillDesign(opts) {
        await this.delay();
        return {
            status: 'success'
        };
    }
    async requestDataFieldMatching(opts) {
        await this.delay();
        return {
            status: 'success'
        };
    }
    constructor(delay){
        this.delay = delay;
    }
}
